version: '3.8'

services:
  backend:
    container_name: finsight-backend
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage
